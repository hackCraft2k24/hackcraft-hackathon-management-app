import 'package:flutter/material.dart';

class SponsorsLanding extends StatelessWidget {
  const SponsorsLanding({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Sponsors',
      style: TextStyle(
        fontSize: 24
      ),),
    );
  }
}

